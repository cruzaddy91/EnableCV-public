# Portfolio PDF Sources

These Markdown files are sanitized source documents for generated PDF evidence
in `assets/pdf/`.

Each source should remain understandable without private context and should use
generic systems, objects, owners, and metrics.
